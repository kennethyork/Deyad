{
  "description": "Tools for repo-wide maintenance.",
  "scripts": {
    "release": "cd deyad-cli && npm run release",
    "format": "prettier --check *.json *.md .github/workflows/*.yml",
    "format:fix": "prettier --write *.json *.md .github/workflows/*.yml"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
